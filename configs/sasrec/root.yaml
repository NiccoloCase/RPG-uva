run_id: sasrec_root
cache_dir: artifacts/sasrec/cache
log_dir: artifacts/sasrec/logs
tensorboard_log_dir: artifacts/sasrec/tensorboard
ckpt_dir: artifacts/sasrec/ckpt

# Shared baseline training protocol from the RPG paper
train_batch_size: 256
eval_batch_size: 32
epochs: 150
patience: 20
eval_interval: 1
weight_decay: 0.0
max_grad_norm: 1.0
